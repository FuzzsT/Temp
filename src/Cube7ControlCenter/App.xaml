<Application x:Class="Cube7ControlCenter.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Startup="App_Startup"
             ShutdownMode="OnExplicitShutdown">
    <Application.Resources>
        <SolidColorBrush x:Key="Bg" Color="#101722"/>
        <SolidColorBrush x:Key="Panel" Color="#182231"/>
        <SolidColorBrush x:Key="Panel2" Color="#202D3E"/>
        <SolidColorBrush x:Key="Accent" Color="#18BFEA"/>
        <SolidColorBrush x:Key="TextPrimary" Color="#F1F6FA"/>
        <SolidColorBrush x:Key="TextSecondary" Color="#9FB1C2"/>
        <Style TargetType="Window">
            <Setter Property="Background" Value="{StaticResource Bg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource Panel2}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Padding" Value="14,9"/>
            <Setter Property="Margin" Value="4"/>
            <Setter Property="BorderBrush" Value="#31445A"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
            <Setter Property="Background" Value="{StaticResource Panel}"/>
            <Setter Property="Padding" Value="16,8"/>
        </Style>
    </Application.Resources>
</Application>
